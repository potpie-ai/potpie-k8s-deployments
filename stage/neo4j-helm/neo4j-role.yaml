apiVersion: rbac.authorization.k8s.io/v1
kind: Role
metadata:
  namespace: neo4j-db
  name: neo4j-role
rules:
- apiGroups: [""]  # "" indicates the core API group
  resources: ["secrets"]
  verbs: ["get", "list", "watch"]
