apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  name: full-access
rules:
- apiGroups: ["*"]    # Access all API groups
  resources: ["*"]    # Access all resources
  verbs: ["*"]        # Allow all verbs (get, list, create, update, delete, etc.)
