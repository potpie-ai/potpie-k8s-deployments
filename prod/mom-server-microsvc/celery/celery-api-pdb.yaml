apiVersion: policy/v1
kind: PodDisruptionBudget
metadata:
  name: celery-api-pdb
  namespace: micro-services
spec:
  minAvailable: 1
  selector:
    matchLabels:
      app: celery-api  # Ensure this matches the labels in your deployment
